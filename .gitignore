# ── Node modules (never commit these — they're huge) ──
node_modules/
frontend/node_modules/
backend/node_modules/

# ── Secret keys (CRITICAL — never commit .env) ──
.env
backend/.env
frontend/.env
frontend/.env.local

# ── Build output ──
frontend/build/

# ── OS junk ──
.DS_Store
Thumbs.db

# ── Editor settings ──
.vscode/settings.json
.idea/

# ── Logs ──
*.log
npm-debug.log*
